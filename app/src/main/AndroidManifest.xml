<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fauzi.infofilm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailActivity"
            android:label="Keterangan Film"></activity>
<activity android:name=".DetailActivity2"
    android:label="Keterangan Film"></activity>
        <activity android:name=".DetailActivity3"
            android:label="Keterangan Film"></activity>
        <activity android:name=".Moviedb"
            android:label="Info Film"></activity>
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>